
<div class="category-name" id="<%= dom_id category %>">
<p class="icon-class">
   <img src="<%= category.icon %>" alt="icon" class="icon-image">
   </p>
   <div class="title-and-date">
 <p class="name-of-category"><%= category.name %></p>
 <p> <%= Time.now.strftime('%m-%d-%Y') %></p>
 </div>
</div>

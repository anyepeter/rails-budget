<%= form_with(model: category_expenditure) do |form| %>
  <% if category_expenditure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_expenditure.errors.count, "error") %> prohibited this category_expenditure from being saved:</h2>

      <ul>
        <% category_expenditure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
